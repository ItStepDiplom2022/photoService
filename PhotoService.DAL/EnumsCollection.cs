namespace PhotoService.DAL
{
    public enum UserRoles
    {
        REGISTERED_USER,
        VERIFIED_USER,
        ADMIN
    }

    public enum CollectionTypes
    {
        LIKES,
        SAVES,
        CUSTOM
    }
}
